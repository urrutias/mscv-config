
spring:
  application:
    name: Config-prod

eureka:
  instance:
    prefer-ip-addres: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
